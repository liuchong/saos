# Basic Example

This directory behaves like a content-only repository using SAOS as a remote
GitHub Action. It covers Markdown, MDX, post-local assets, site configuration,
Giscus configuration, and deployment below a URL base path.

Build it from the repository root:

```sh
npm run build
```
